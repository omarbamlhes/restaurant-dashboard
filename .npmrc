auto-install-peers=true
strict-peer-dependencies=false
# pnpm v10 refuses `pnpm deploy` from non-injected workspaces by default;
# the API Dockerfile relies on it, so opt back into the legacy behavior.
force-legacy-deploy=true
